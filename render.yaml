services:
  - type: web
    name: ai-journal-backend
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm run start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 5000
